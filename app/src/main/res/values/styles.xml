<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo" />

    <style name="AppTheme.Transparent" parent="android:Theme.Translucent.NoTitleBar" />

</resources>
